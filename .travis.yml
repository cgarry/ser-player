sudo: required
dist: trusty
language: c++

install:
  - sudo apt-get install qt5-qmake
  - sudo apt-get qtbase5-dev

script:
  - cd ser-player
  - export QT_SELECT=5
  - qmake CONFIG+=release
  - make
